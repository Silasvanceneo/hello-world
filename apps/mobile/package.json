{
  "name": "@hello-world/mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "android:debug": "npm run sync && cd android && gradlew.bat assembleDebug",
    "build": "npm run sync && node ../../scripts/verify-structure.mjs",
    "sync": "npm --workspace @hello-world/web run build && cap sync android"
  },
  "dependencies": {
    "@aparajita/capacitor-secure-storage": "^8.0.0",
    "@capacitor/android": "^8.3.1",
    "@capacitor/camera": "^8.2.0",
    "@capacitor/core": "^8.3.1",
    "@capacitor/preferences": "^8.0.1",
    "@capacitor/share": "^8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.1"
  }
}
